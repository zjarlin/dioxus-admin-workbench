# 后台页面与集合列表

无业务依赖的页面标题、反馈、列表搜索、排序、分页和多选。领域插件提供数据、列及动作，不在本组件访问网络或持久化。

`ListState` 和 `SortValue` 不依赖 UI。`CollectionTable` 只对传入的完整集合进行本地查询，不假装提供服务端分页；大量数据应由插件提供查询接口。选择以稳定行 ID 关联，表头只选择当前页，过滤和翻页不会隐式扩大选择范围。

主题、字号、间距、焦点、触控和表格横滚由共享样式负责。实际 CRUD、租户隔离与授权始终由插件后端验证。
